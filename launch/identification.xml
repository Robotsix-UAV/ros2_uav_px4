<launch>
    <!-- Define a launch argument for the namespace -->
    <arg name="uav_namespace" default="/uav0"/>

    <!-- Node for thrust matcher -->
    <node pkg="ros2_uav_px4"
          exec="thrust_matcher"
          namespace="$(var uav_namespace)"
          output="screen">
    </node>
</launch>
